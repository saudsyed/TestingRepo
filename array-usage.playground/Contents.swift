import UIKit

//Multiple employees may take too many lines of code

var employee1Salary = 45000.0
var employee2Salary = 60000.0
var employee3Salary = 100000.0
var employee4Salary = 15000.0

//  arrayName        = [multipleElements || arrayIndex]
var employeeSalaries = [45000.0, 60000.0, 100000.0, 15000.0]

// arrayName.append is use to add more multiple elments in ARRAY.
employeeSalaries.append(52000)

// arrayName.remove is use to remove arrayIndex
employeeSalaries.remove(at: 2)
